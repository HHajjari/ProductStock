package com.xyz.productStock;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * The class implements entry point of the application
 * @version 1.0
 * @author Hossein Hajjari
 */
@SpringBootApplication
public class ProductStockApplication {

	/**
	 * The main method is the startin point of system
	 * @param args represents input arguments
	 */
	public static void main(String[] args) {
		SpringApplication.run(ProductStockApplication.class, args);
	}

}
